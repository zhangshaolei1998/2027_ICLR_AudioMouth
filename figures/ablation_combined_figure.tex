\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{figures/alternatives/ablations_reference100_combined.pdf}
    \caption{Ablations of (a) linguistic conditioning, (b) coefficient reweighting, and (c) motion refinement the selected test data. Bar heights are $100E/E_{\mathrm{ref}}$, with each panel's reference marked in its legend; lower is better. Numeric labels show measured values rounded to one decimal place, with MSE multiplied by $10^4$ and MAE by $10^3$. FD, WInD, and LMD are script means. All variants use the same post-processing setting.}
    \label{fig:ablations}
\end{figure}
